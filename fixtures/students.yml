App\Entity\User\Student:
  student (template):
    firstName: '<firstName()>'
    lastName: '<lastName()>'

  student1 (extends student):
    email: 'student1.symfony.lectorium@appcreative.net'
    password: "<encodePassword(@self, 'student1')>"
    courses: ['@course1']

  student{2..50} (extends student):
    email: '<email()>'
    password: "<encodePassword(@self, 'student')>"
    courses: ['@course1']